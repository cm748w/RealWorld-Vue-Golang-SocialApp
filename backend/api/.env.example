JWT_SECRET=your_key_here

# Default page size for post listings when the request does not send `limit`.
POSTS_PAGE_SIZE=2

# Maximum allowed `limit` for post listings to prevent fetching too many posts at once.
POSTS_MAX_PAGE_SIZE=50