# MongoDB 连接 URI (默认: mongodb://localhost:27017)
MONGO_URI=mongodb://localhost:27017

# JWT 密钥，用于生成和验证令牌
JWT_SECRET=your-secret-key

# 当请求未指定 `limit` 参数时，帖子列表的默认每页数量
POSTS_PAGE_SIZE=2

# 帖子列表允许的最大 `limit` 值，防止一次获取过多帖子
POSTS_MAX_PAGE_SIZE=50