# dependencies
node_modules/
# 项目使用 pnpm，yarn.lock 不应进入仓库（残留会导致 vue-cli 误判为 yarn 项目）
yarn.lock

# production build output
dist/

# runtime temp dir used by test-sim/module-test.mjs (copies of src modules)
test-sim/

# # local env files
# .env.local
# .env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor and OS files
.DS_Store
Thumbs.db
